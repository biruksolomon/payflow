spring:

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

  h2:
    console:
      enabled: true

  flyway:
    enabled: false

app:
  jwt:
    secret-key: test-secret-key-that-is-at-least-256-bits-long-for-testing
    expiration: 86400000
    refresh-token-expiration: 604800000
  super-admin:
    email: superadmin@test.com
    password: SuperAdmin@Test123
    first-name: Super
    last-name: Admin

  stripe:
    api-key: sk_test_testApiKey
    public-key: public-key
    webhook-secret: whsec_testWebhookSecret

